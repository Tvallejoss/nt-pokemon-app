/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "www.pokemon.com", // Agrega solo el dominio
        ],
    },
};

export default nextConfig;
